# Initial setup: docs/mcp-setup.md ("First-time setup")
# PHORGE_URL=http://localhost:4201/mcp
# random secret, e.g.: node -e "console.log(require('crypto').randomBytes(24).toString('hex'))"
PHORGE_TOKEN=
# from `claude setup-token` (subscription login as a long-lived token)
CLAUDE_CODE_OAUTH_TOKEN=
