name: Lint & Test with coverage (vitest)

on:
  push:
  workflow_dispatch:
  pull_request:

jobs:
  # Fail-fast lint gate: oxlint runs in seconds with no install step, so a lint
  # regression surfaces almost immediately — well before the slower test job below
  # finishes. Pinned version + --deny-warnings keeps it identical to local `oxlint`.
  oxlint:
    name: Lint (fast fail)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npx --yes oxlint@1.72.0 --deny-warnings
  test:
    runs-on: ubuntu-latest
    # no version matrix: a single Node version keeps the check name stable
    # ("Test & Typecheck") for the required-checks branch protection on master
    name: Test & Typecheck
    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js 24
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install dependencies
        run: npm ci
      - name: Typecheck - TSC
        run: npm run tsc
      - name: ESLint
        run: npm run lint
      - name: Unit tests with coverage (app + engine) - Vitest
        run: npm run coverage
        env:
          CI: true
