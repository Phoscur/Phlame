{
  "mcpServers": {
    "phlame-game": {
      "command": "npx",
      "args": ["tsx", "tools/mcp/server.ts"]
    },
    "phorge": {
      "url": "${PHORGE_URL}",
      "headers": { "Authorization": "Bearer ${PHORGE_TOKEN}" }
    }
  }
}
